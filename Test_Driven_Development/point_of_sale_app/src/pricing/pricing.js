export default class PricingService {
    getItem(id){
        // Imagine a call to a third-party REST-api here. This method currently
        // throws an error to ensure that it is not called directly.
        throw new Error("getItem method directly invoked but is not implemented!")
    }
}
