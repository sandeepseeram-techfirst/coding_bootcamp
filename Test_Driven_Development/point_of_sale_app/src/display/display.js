export default class Display {
    displaySale(price, itemName) {
        // Imagine, if you will, fancy hardware integration
        // to manipulate a physical screen. This method currently
        // throws an error to ensure that it is not called directly.
        throw new Error("displaySale method directly invoked but is not implemented!")
    }
}
